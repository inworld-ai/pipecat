- Fixed `DeepgramTTSService` bypassing the audio context queue, which caused stale audio frames to leak into the pipeline after interruptions. Audio is now routed through `append_to_audio_context()` and interruptions use the `on_audio_context_interrupted()` hook, matching the pattern used by ElevenLabs and Cartesia.
